server {
    listen 80;
    listen [::]:80;

    # Allow Certbot to validate the domain
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other traffic to HTTPS (Uncomment AFTER generating certs)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # Current HTTP setup (Delete or comment this out AFTER generating certs)
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# SSL Configuration (Uncomment this block AFTER generating certs)
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name example.com; # REPLACE THIS with your domain
#
#     ssl_certificate /etc/nginx/ssl/live/example.com/fullchain.pem; # REPLACE example.com
#     ssl_certificate_key /etc/nginx/ssl/live/example.com/privkey.pem; # REPLACE example.com
#
#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://backend:8000/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
